@* @model Student_Section_ManagementSystemProject.Models.Schedule

@{
    ViewData["Title"] = "Enroll Students";
}

<h2>Enroll Students in @Model.Subject.Name</h2>

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success">@TempData["SuccessMessage"]</div>
}

@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger">@TempData["ErrorMessage"]</div>
}

<form method="post" asp-action="Enroll">
    <input type="hidden" name="scheduleId" value="@Model.Id" />

    <div class="form-group">
        <label>Select Student</label>
        <select name="studentId" asp-items="ViewBag.Students" class="form-control"></select>
    </div>

    <button type="submit" class="btn btn-primary">Enroll</button>
</form>

<h3>Enrolled Students</h3>
<table class="table">
    <thead>
        <tr>
            <th>Student Name</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var enrollment in Model.Enrollments)
        {
            <tr>
                <td>@enrollment.Student.Name</td>
                <td>
                    <form method="post" asp-action="Unenroll">
                        <input type="hidden" name="scheduleId" value="@Model.Id" />
                        <input type="hidden" name="studentId" value="@enrollment.Student.Id" />
                        <button type="submit" class="btn btn-danger">Remove</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
 *@